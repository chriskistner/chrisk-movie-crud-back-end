const express = require('express');
const router = express.Router();
const ctrl = require('../controllers/actors-controllers');

// router.get('/actor/', ctrl.allActors);
// router.get('/actor/:id', ctrl.getActor);
// router.delete('/actor/:id', ctrl.deleteActor);
// router.post('/actor/', ctrl.newActor);
// router.put('/actor/:id', ctrl.updateActor);

module.exports = router